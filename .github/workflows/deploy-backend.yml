name: Deploy Backend to Render

on:
  push:
    branches: [main]
    paths:
      - '**.py'
      - 'questions.json'
      - '.github/workflows/deploy-backend.yml'

jobs:
  render-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy Hook
        run: curl -X POST https://api.render.com/deploy/srv-d21jrvvfte5s73fmstlg?key=5kfvKMt6fPg
